# homebrew-tap

Homebrew tap for [Floom CLI](https://floom.dev).

## Install

```sh
brew install floomhq/tap/floom
```

Or tap first, then install:

```sh
brew tap floomhq/tap
brew install floom
```

## Update

```sh
brew upgrade floom
```
